% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cmd.R
\name{commands}
\alias{commands}
\alias{cmd_quit}
\alias{cmd_tick}
\alias{cmd_run}
\alias{cmd_msg}
\alias{cmd_batch}
\title{Commands}
\usage{
cmd_quit()

cmd_tick(interval)

cmd_run(fn)

cmd_msg(msg)

cmd_batch(...)
}
\arguments{
\item{interval}{Seconds until the tick fires.}

\item{fn}{A function \verb{() -> msg}. Its return value, unless \code{NULL}, is fed
back through \code{update} as a message.}

\item{msg}{A message to dispatch.}

\item{...}{Commands to combine; \code{NULL}s are dropped.}
}
\description{
Commands are how \code{update} asks the runtime to \emph{do} something: quit, schedule
a tick, run an effect, or several at once. \code{update} returns a command (or
\code{NULL}) alongside the new model; the runtime carries it out and feeds any
resulting message back through \code{update}.
}
\details{
\itemize{
\item \code{\link[=cmd_quit]{cmd_quit()}} stops the application.
\item \code{\link[=cmd_tick]{cmd_tick()}} asks for a \link{TickMsg} after \code{interval} seconds.
\item \code{\link[=cmd_run]{cmd_run()}} runs an arbitrary effect \verb{() -> msg}.
\item \code{\link[=cmd_msg]{cmd_msg()}} dispatches a message immediately.
\item \code{\link[=cmd_batch]{cmd_batch()}} combines several commands into one.
}

Effects run synchronously on the runtime loop, so a long-running \code{\link[=cmd_run]{cmd_run()}}
blocks input and redraws until it returns.
}
